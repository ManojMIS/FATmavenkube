apiVersion: apps/v1
kind: Deployment
metadata:
  name: maven-webapp
  labels:
    app: maven
spec:
  replicas: 2
  selector:
    matchLabels:
      app: maven
  template:
    metadata:
      labels:
        app: maven
    spec:
      containers:
      - name: web-container
        image: manojkumar2023/maven-app:latest # Replace with your DockerHub tag
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: maven-service
spec:
  type: NodePort
  selector:
    app: maven
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007 # The application will be visible at <NodeIP>:30007